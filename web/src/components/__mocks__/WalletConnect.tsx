import React from 'react';

export function WalletConnect({ className }: { className?: string }) {
  return (
    <div data-testid="mock-wallet-connect" className={className}>
      Mock WalletConnect Component
    </div>
  );
} 